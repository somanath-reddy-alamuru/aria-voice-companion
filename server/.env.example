GEMINI_API_KEY=your-key-here
PORT=3001

# Optional — without this, "play X on YouTube" opens search results instead
# of the exact video. Get a free key at https://console.cloud.google.com
# (enable "YouTube Data API v3", then create an API key under Credentials).
YOUTUBE_API_KEY=
